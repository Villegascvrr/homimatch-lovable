/**
 * LEGACY FILE - NO LONGER IN USE
 * This file is kept for reference but is not used in the simplified landing version
 * All matching functionality has been replaced with MatchingLandingPage.tsx
 */

import { Navigate } from 'react-router-dom';

const MatchingPageLegacy = () => {
  return <Navigate to="/matching" replace />;
};

export default MatchingPageLegacy;
